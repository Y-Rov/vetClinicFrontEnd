import { NullValueDirective } from './null-value.directive';

describe('NullValueDirective', () => {
  it('should create an instance', () => {
    const directive = new NullValueDirective();
    expect(directive).toBeTruthy();
  });
});
